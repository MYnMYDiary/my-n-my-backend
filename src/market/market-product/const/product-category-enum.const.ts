/**
 * 마켓 상품 카테고리
 * @property {string} `STICKER` 스티커 STI
 * @property {string} `MASKING_TAPE` 마스킹테이프 MTE
 * @property {string} `POSTCARD` 엽서 PCD
 * @property {string} `NOTEPAD` 노트 NTP
 */
export enum MarketProductCategory {
    STICKER = 'STI', // 스티커
    MASKING_TAPE = 'MTE', // 마스킹테이프
    POSTCARD = 'PCD', // 엽서
    NOTEPAD = 'NTP', // 노트
}